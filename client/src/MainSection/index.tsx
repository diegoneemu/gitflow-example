import React from "react";

interface MainSectionProps {}

const MainSection = (props: MainSectionProps): JSX.Element => {
  return <section id="main-section"></section>;
};

export default MainSection;
